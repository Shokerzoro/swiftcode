#pragma once

#include <array>
#include <string>
#include <utility>

// CodeGen need for ResourceTypePairs.
enum class ResourceType {
    Employees = 10,
    Production = 11,
    Integration = 12,
};
